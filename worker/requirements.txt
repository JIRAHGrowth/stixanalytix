# Local-only deps for the Modal CLI.
# The Modal worker image installs its own deps (google-generativeai, supabase,
# requests) inside the Linux image — see the IMAGE block in worker/app.py.
# The full supabase client pulls in pyiceberg which doesn't ship Windows
# wheels for recent Python versions; we don't need it locally so we skip it.
modal>=1.0.0
python-dotenv>=1.0.0
requests>=2.32.0
# fastapi is only needed locally so `@modal.fastapi_endpoint` can introspect
# `Header(...)` defaults at deploy time. The Modal image installs its own copy.
fastapi>=0.115.0
